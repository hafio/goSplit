# Binaries
/gosplit
/out/
*.exe

# Release build output (dev.sh/dev.ps1 `build` writes dist/gosplit-<os>-<arch>)
/dist/

# Local data (SQLite file, WAL, uploads)
/data/
*.db
*.db-shm
*.db-wal

# Env
.env

# Coverage / test artifacts (written to scripts/logs/ by the cov task)
coverage.out
coverage.html
# Stray coverage profile from a mis-run cov command (unexpanded $prof variable)
$prof

# Dev script logs (includes coverage.out / coverage.html)
scripts/logs/

# Golden fixtures are committed; local scratch is not
*.local.json

# OS / editor
.DS_Store
Thumbs.db

#Notepad++
nppBackup

graphify-out/20*