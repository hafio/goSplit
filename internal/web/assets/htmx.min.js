/* Placeholder for htmx. The app uses progressive enhancement: all core flows
 * are plain server-rendered forms and links that work WITHOUT JavaScript.
 * To enable htmx fragment swaps, replace this file with the real htmx build
 * (https://unpkg.com/htmx.org/dist/htmx.min.js) — it is loaded via
 * /static/htmx.min.js and needs no other wiring. */
